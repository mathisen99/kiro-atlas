# Create Quests Requirements

## Create a quest

The user can create a quest with a non-empty title. Each new quest receives an identifier and starts incomplete.

## Complete a quest

The user can mark an existing quest complete. Completing one quest must not change any other quest.

## Keep the demo understandable

Quest data remains in memory and uses the shared `Quest` type from `demo/quests.ts`.
